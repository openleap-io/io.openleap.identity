spring:
  profiles:
    active: test
  cloud:
    config:
      enabled: false
  config:
    import: classpath:config/identity-service-default.yml
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false